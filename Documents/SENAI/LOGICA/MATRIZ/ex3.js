const numbers = [
  [10, 20, 60],
  [8, 10, 52],
];

for (let x = 0; x < numbers.length; x++) {
  for (let y = 0; y <= numbers.length; y++) {
    console.log("A matriz possui o valor", numbers[x][y]);
  }
}
